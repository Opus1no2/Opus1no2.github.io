{
  "name": "TT",
  "version": "1.0.0",
  "description": "Travis Website",
  "author": "Travis Tillotson",
  "license": "MIT",
  "private": false,
  "scripts": {
    "css": "./node_modules/.bin/cleancss -o styles/main.css src/styles/*.css",
    "images": "cp src/images/* images/",
    "build": "yarn css && cp src/index.html index.html",
    "serve": "yarn build && python -m SimpleHTTPServer 8000"
  },
  "dependencies": {
    "clean-css-cli": "^4.1.10",
    "normalize.css": "^7.0.0"
  }
}
